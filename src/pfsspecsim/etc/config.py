"""Instrument configuration file parser -> `Spectrograph` dataclass.

Ports the `PFS.*.dat` spectrograph-attribute file reader (gsetc.c's
`gsReadSpectrograph` and friends). Not yet implemented; lands with the
config/params task.
"""
