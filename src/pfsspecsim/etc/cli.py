"""`typer` CLI app (`pfs-etc`) for the pure-Python ETC engine.

CLI options take priority over a TOML config file, which in turn takes
priority over `EtcParams` defaults (see `params.load_params`). Not yet
implemented.
"""
